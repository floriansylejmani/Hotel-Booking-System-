# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:5259/api

# Docker Compose / API
# Use a unique 32+ character value locally. Do not commit real secrets.
JWT_SECRET=replace-with-a-strong-local-development-secret

# Optional local PostgreSQL defaults for development only.
POSTGRES_DB=hotel_booking
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
